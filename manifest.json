{
  "manifest_version": 3,
  "name": "Wikidata Entity ID Linker Extension",
  "version": "1.0",
  "description": "An extension that creates web links for matching HTML text based on a regex format of \\sQ\\d+ which is the format of a Wikidata Entity ID.",
  "permissions": ["activeTab"],
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["linker.js"]
    }
  ]
}
